package com.desensitize.core.model;

public enum MaskFormatType {
    PRESERVE,
    REPLACE,
    MASK_ALL,
    NAME_MASK,
    EMAIL_MASK,
    DATE_MASK,
    LANDLINE_MASK,
    ADDRESS_MASK,
    PASSPORT_MASK
}
